import styled from "styled-components";

const Container = styled.div`
  min-height: 100vh;
  width: calc(100% - 350px);
  padding: 50px;

  position: absolute;
  top: 0;
  right: 0;

  color: white;
`;

export { Container };
